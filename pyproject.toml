[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "autoresearch-v2"
version = "0.1.0"
description = "AutoResearch v2 implementation bootstrap"
requires-python = ">=3.10"

